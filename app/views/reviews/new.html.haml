%h2 Create a Review

= form_tag music_reviews_path, :method => :post do

    = label :review, :musicman, 'Vote'
    = select :review, :musicman, [1,2,3,4,5]
    
    = label :review, :comments, 'Comment'
    = text_field :review, :comments
    
    

    =submit_tag 'Add a review'
